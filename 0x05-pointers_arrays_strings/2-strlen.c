#include "main.h"
/**
 * _strlen - function that measures length of strimgs
 * @s: the pointer to the string
 * Return: nothing
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; *(s + i) != '\0'; ++i)
	{

	}
	return (i);
}
